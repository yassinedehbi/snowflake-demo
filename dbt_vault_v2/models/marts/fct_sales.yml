version: 2

models:
  - name: fct_sales
    description: "This fact table combines data from the sales, location, and product satellite tables to provide a comprehensive view of sales transactions."
    columns:
      - name: transaction_sales
        tests:
          - unique
          - not_null